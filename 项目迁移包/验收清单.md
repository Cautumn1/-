# 新项目接续验收清单

## 上下文

- [ ] 新项目根目录能看到 `AGENTS.md` 和 `PROJECT_CONTEXT.md`。
- [ ] 新对话已经明确读完这两个文件。
- [ ] 新对话知道旧 `README.md` / `CODEX_DEPLOY_PROMPT.txt` 不是当前需求。

## 安全与 CloudBase

- [ ] 唯一环境为 `a1-d4gbxgxqmc01c0e55`，没有创建新环境。
- [ ] 未开通、升级或切换任何可能收费的资源。
- [ ] 套餐/超额状态在执行线上操作前重新确认。
- [ ] `.env.production` 和访问密钥没有进入聊天、压缩包或公开仓库。
- [ ] `groups`、`members`、`sessions`、`checkins` 仍为客户端不可读写。
- [ ] 默认函数禁止调用，仅认证用户可调用 `study-checkin-api`。

## 功能

- [ ] 蔡/刘可直接选择和切换，手机端按钮可见。
- [ ] 双方任务可编辑、增删、上移/下移并互相查看。
- [ ] 排序后完成记录与学习时长不串到其他任务。
- [ ] 普通自习和番茄钟均可开始、暂停、继续、结束。
- [ ] 自己和对方的今日自习总时长都正确。
- [ ] 页面可见时每 3 分钟同步，隐藏时停止，无次数上限。
- [ ] 励志语录每次打开/刷新更换且不请求数据库。

## 构建与部署

- [ ] 在实际前端工程内先运行 `npm install`。
- [ ] `npm run build` 成功。
- [ ] 只上传 `dist` 到现有静态托管根目录。
- [ ] 云函数如有修改，仍部署为 Node.js 18+ 的 `study-checkin-api` 并安装依赖。
- [ ] 正确网址可在桌面端和手机端访问。
