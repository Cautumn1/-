# “一起进步｜双人学习打卡”完整项目上下文

> 交接快照日期：2026-08-06（Asia/Hong_Kong）  
> 用途：把本次长对话中对继续开发真正有用的需求、决策、现状、部署边界和历史修复迁移到新的 Codex/ChatGPT 项目。  
> 说明：这是结构化交接，不是聊天气泡的逐字导出；它以当前最终需求为准，早期且已被后续要求推翻的方案会明确标为历史。

## 1. 一句话目标

维护一个仅供蔡和刘使用的双人学习打卡网页：双方可以直接选择身份、编辑每日任务、打卡、查看彼此任务和进度，并通过普通自习或番茄钟看到对方的实时学习状态；同时把 CloudBase 资源点消耗控制在很低的水平。

## 2. 绝对约束

1. 这是已有项目的迁移部署和持续迭代，不重新设计整套页面，不擅自改业务逻辑。
2. 唯一允许使用的 CloudBase 环境 ID 是 `a1-d4gbxgxqmc01c0e55`。早期写成 `a1-d4gbxqxqmc01c0e55` 的 ID 是抄写错误，禁止使用，也禁止创建新环境。
3. 不开通、不升级、不切换任何付费资源。若操作可能收费，必须先停下并告诉用户。
4. 前端是 Vite + React 项目。标准流程始终是先 `npm install`，再 `npm run build`，然后把 `dist` 上传到现有 CloudBase 静态网站托管。
5. NoSQL 集合固定为 `groups`、`members`、`sessions`、`checkins`。客户端不得直接读写；全部业务数据经 `study-checkin-api` 云函数访问。
6. 匿名登录保持启用。函数权限保持默认禁止，只允许已认证用户调用 `study-checkin-api`；不要开放其他函数或数据库客户端权限。
7. 任何文档、聊天、压缩包和代码提交都不得泄漏 `.env.production` 中的访问密钥。
8. 页面文案、用户名称、任务语义、稳定任务 ID、自习同步和低消耗策略都应按本文当前状态维护。

## 3. 当前用户体验

### 3.1 身份

- 用户一显示名：`蔡`
- 用户二显示名：`刘`
- 进入后直接选择蔡或刘，不需要共享码、邀请码、PIN 或密码。
- 桌面端和手机端都必须能看到并使用身份切换按钮。

### 3.2 每日任务

- “一天”统一按北京时间早上 `05:00` 划分：当天 `05:00:00` 至次日 `04:59:59` 属于同一个学习日；凌晨 0 点到 4:59 的任务、打卡和自习仍计入前一个日期。
- 每个人可以编辑自己当天的任务，最多 12 项。
- 编辑任务时可开启“每天重复”。开启后当前任务列表会从当前学习日起作为个人每日模板，未来没有单日覆盖时自动沿用；关闭后只保存当前学习日并停止以后重复。
- 可以新增、删除、上移、下移并保存任务。
- 任务卡左侧使用 `1、2、3、4……` 数字，不使用无意义的装饰图案。
- 双方可以看到对方当天任务、完成状态、任务进度和已记录的任务学习时间。
- “今日进度”和大任务卡已经弱化，自习模式和对方自习状态是页面视觉重点。

蔡在 2026-08-02 修复后的任务快照为：

| 顺序 | 稳定 ID | 标题 |
|---:|---|---|
| 1 | `review` | 英语单词复习 |
| 2 | `words-1` | 英语单词 第一节 |
| 3 | `computer-2` | 计算机二级 |
| 4 | `civil-service` | 考公 |
| 5 | `words-2` | 英语单词第二节 |

刘的任务由刘自行编辑；2026-08-02 的线上快照曾为“英语单词课、内科、英语语法课”，它不是永久固定模板。

### 3.3 自习模式

- 主界面直接展示“自习模式”和“对方的自习状态”，不藏进侧边栏或多级入口。
- 点击“开始自习”后，先选择本次关联的任务，也可以选择自由自习。
- 选择任务后可选择普通自习或番茄钟。
- 番茄钟的专注时长和休息时长可以自由设置。
- 普通自习和番茄钟都支持：开始、暂停、继续、结束。
- 自己的卡片显示当前计时、状态、本次任务和“今日累计”；进行中时今日累计应包含当前这段有效学习时间，暂停时冻结。
- 对方卡片显示：是否正在学习、是否暂停、当前任务/自由自习、当前计时、今日自习总时长和今日任务完成数。
- 页面陪伴文案当前为：`随时开始，对方会看到你的学习状态`。
- 已删除文案：`你先开始后，刘打开页面就能看到你的陪伴`。

### 3.4 励志语录

- 页面主标题不再固定为“今天，也向目标靠近一点”。
- 文件 `励志语录候选-1000条.md` 包含 1000 条更适合大学生的学习/成长语录。
- 前端以原始文本方式在构建期导入该文件，不访问数据库或云函数。
- 每次真正打开或刷新网页时随机显示一条；同一页面生命周期内不会因 React 重渲染而变化。
- 使用本地存储键 `study-last-motivational-quote` 避免紧接着重复上一条。

### 3.5 已删除或不得恢复的界面内容
- 不显示：`不需要共享码或密码，直接选择自己的身份。`
- 不显示：`已有固定学习计划`
- 不显示：`任务待定，请自行添加`
- 不显示：`今日自定义任务`
- 不显示：`你先开始后，刘打开页面就能看到你的陪伴`
- 不恢复旧的共享码/PIN 创建和加入界面。

### 3.6 版本与更新日志

- 当前前端版本为 `v1.6.0`。
- 前端静态维护版本号和历史更新日志，不访问数据库。
- 每次发布新版本后，同一浏览器第一次打开或刷新页面时自动弹出更新日志；用户确认后用本地存储键 `study-last-seen-version` 记录已读版本，该版本不再重复弹出。
- 已补录 `v1.0.0` 至当前版本的主要更新记录；以后发布时同步递增版本号并把更新内容追加到前端日志列表。
- 前端还会读取同源静态文件 `/version.json` 检测线上版本；发现版本号与当前页面不同会直接弹出“网站有更新”界面，提示用户刷新网址。该检测不调用云函数或数据库。

### 3.7 侧边栏与历史统计

- 桌面端使用固定侧边栏，手机窄屏使用可展开的抽屉侧边栏。
- 侧边栏提供“今日学习”“历史统计”和“切换用户”入口；蔡/刘身份切换不再占用页面顶部空间。
- 历史统计以按月日历显示，每一天直接显示已记录的学习时长；当天全部任务完成时在日期上打勾。
- 原独立的“本周坚持”面板已经取消，其完成反馈并入历史日历。
- 历史月历只在进入历史统计或切换月份时按需读取一个 42 天日历网格，不参与常规轮询。

## 4. 同步规则与资源点策略

这是当前权威同步规则：

1. 页面首次进入并选定身份时，读取完整页面数据。
2. 页面可见时，无论当前是否有人计时，每 3 分钟调用一次轻量的 `getFocusStatus`。
3. 自动刷新没有次数上限。
4. 浏览器标签页隐藏或手机页面进入后台时，不自动轮询；重新回到可见状态时会尝试刷新。
5. 页面重新获得焦点时会尝试刷新，但有 60 秒节流，避免切窗口造成请求风暴。
6. 用户点击手动刷新时可以立即读取需要的数据。
7. 自己的秒表和番茄钟每秒只在浏览器本地更新，不产生逐秒函数调用、数据库读取或数据库写入。
8. 开始、暂停、继续、结束自习时各进行一次必要的云函数写入/读取。
9. 打卡、保存/恢复任务、选择身份等明确用户动作才访问后端。
10. 励志语录完全随前端静态资源加载，不消耗函数或数据库资源点。
11. 轻量 `getFocusStatus` 会同时返回汇总修订号；结束自习、打卡、保存或重置任务时才更新该修订号。前端发现修订号变化后补做一次完整数据刷新，因此对方的今日累计、任务完成和任务学习时长能自动更新，同时常规 3 分钟轮询仍只读取轻量状态文档。
12. “刷新对方自习状态”和“刷新对方任务”两个手动入口都会立即读取完整页面数据，不受 60 秒自动刷新节流限制。
13. 学习日以北京时间 `05:00` 为界。页面保持打开时会用本地定时器在 05:00 切换学习日并读取一次完整数据；页面在后台时不请求，回到前台后发现学习日变化会立即刷新，不受 60 秒节流限制。
14. 更新日志和已读版本完全保存在前端静态资源与浏览器本地存储中，不消耗函数或数据库资源点。
15. `/version.json` 使用现有 3 分钟可见页节奏检测；页面隐藏时停止，恢复可见时检测。它是静态托管请求，不消耗云函数或数据库资源点。
16. 历史日历通过 `getStudyHistory` 按月按需加载，一次最多读取固定 42 天范围；不使用实时监听，也不随每次轻量状态轮询重复读取。

按 3 分钟间隔计算，一个持续可见的客户端理论上每小时最多产生约 20 次周期性状态刷新；两个客户端同时可见 16 小时约为 640 次周期性刷新，另加首次载入、手动刷新和用户操作。不要把“函数调用次数”直接当成“资源点数”：实际资源点还取决于 CloudBase 当期计费换算、函数执行时长和数据库操作。任何新的估算都应重新查官方当期计费文档。

后续优化必须遵守：

- 不增加 WebSocket/实时数据库监听，除非先证明它比当前轮询更省且用户明确同意。
- 不做秒级/十秒级后端心跳。
- 不把本地倒计时持续写入数据库；云端只保存开始时间、暂停累计、模式和结束结果等状态。
- 能从时间戳在客户端推导的值，不重复从云端读取或每秒写入。
- 能随静态包发布的数据（如语录），不放数据库。
- 避免因窗口聚焦、可见性切换和 React 重渲染发出重复请求。

## 5. 任务稳定 ID 与历史数据修复

完成记录和任务学习时长都按 `taskId` 关联，而不是按标题或数组位置关联。因此：

- 编辑标题时保留原任务 `id`。
- 上移/下移时只交换任务对象在数组中的顺序，任务对象自己的 `id` 不变。
- 新增任务才生成新 ID。
- 不允许通过“把标题重新填到不同位置”来模拟排序，否则完成记录和学习时长会跟随旧 ID 错位。

2026-08-02 曾发生的故障：原顺序为“英语单词 第一节、计算机二级、考公、英语单词第二节、英语单词复习”，用户为了改顺序重新编辑了各位置标题，导致已完成的“计算机二级”和“英语单词复习”显示到其他标题上。

已完成的修复：

- 编辑弹窗增加上移/下移按钮，排序会保留稳定 ID。
- 云端当日任务的 ID/标题语义已修正为本文表格。
- 当日打卡记录修正为“英语单词复习”和“计算机二级”完成，并保留原完成时间。
- 自习记录关联的任务 ID 已修正；总学习时长没有改变。
- 当日修复时的学习时长约为：英语单词复习 19 分钟、计算机二级 58 分钟。它只是历史验收记录，不是每天固定数值。

## 6. 技术架构

### 6.1 本地目录

```text
study-checkin-cloudbase/                 工作区根目录
├─ AGENTS.md                              Codex 自动协作规则
├─ PROJECT_CONTEXT.md                     本交接文件
├─ 励志语录候选-1000条.md                 1000 条静态语录
├─ 项目迁移包/                            给新项目使用的说明与压缩包素材
└─ study-checkin-cloudbase/               实际 Vite React 工程
   ├─ src/App.tsx                         页面、状态、同步、计时、任务编辑
   ├─ src/styles.css                      桌面/手机响应式样式
   ├─ src/cloudbase.ts                    匿名登录与云函数调用
   ├─ functions/study-checkin-api/index.js 后端业务逻辑
   ├─ functions/study-checkin-api/package.json
   ├─ package.json
   ├─ .env.production                     本地生产配置，包含敏感值，不得打包
   └─ dist/                               构建产物
```

### 6.2 前端版本

- React `19.2.6`
- React DOM `19.2.6`
- Vite `8.0.13`
- TypeScript `5.9.3`
- `@cloudbase/js-sdk` `3.7.0`

构建脚本为 `tsc --noEmit && vite build`。

### 6.3 云函数

- 名称：`study-checkin-api`
- 类型：普通云函数
- 运行时：`Nodejs18.15`（满足 Node.js 18 或更高）
- 入口：`index.main`
- 内存：256 MB
- 超时：30 秒
- 依赖安装：开启
- 本地依赖：`@cloudbase/node-sdk` `3.18.3`
- 2026-08-02 查询状态：`Active`

支持的动作：

- `selectUser`
- `getData`
- `getFocusStatus`
- `getStudyHistory`
- `toggleCheckin`
- `saveTasks`
- `resetTasks`
- `startFocus`
- `setFocusPaused`
- `stopFocus`

`getData` 只读取当前学习日；历史统计由 `getStudyHistory` 单独按需读取。自习状态文档保留最近 730 天的每日总时长汇总，同时继续只保留最近 60 天的详细任务时长，以控制文档体积并兼容旧数据。

### 6.4 数据库

使用 CloudBase NoSQL 文档数据库，不使用 PostgreSQL 或 MySQL。集合与索引：

| 集合 | 必要索引 | 客户端权限 |
|---|---|---|
| `groups` | `code` 升序普通索引 | `ADMINONLY` |
| `members` | `groupId` 升序普通索引 | `ADMINONLY` |
| `sessions` | 系统索引 | `ADMINONLY` |
| `checkins` | `groupId` 升序 + `day` 降序复合索引 | `ADMINONLY` |

2026-08-02 已通过 CloudBase MCP 再次确认四个集合均为 `ADMINONLY`。

### 6.5 身份和函数权限

- 匿名登录已启用。
- 前端先获得匿名身份，再调用云函数。
- 2026-08-02 验证的函数安全规则等价于：默认所有函数禁止调用，仅 `study-checkin-api` 允许 `auth != null` 的用户调用。
- 前端 `src/cloudbase.ts` 只做匿名认证和函数调用，不应增加客户端数据库访问。

## 7. CloudBase 线上环境快照

- 环境 ID：`a1-d4gbxgxqmc01c0e55`
- 环境别名：`a1`
- 地域：`ap-shanghai`
- 环境状态（2026-08-02）：`NORMAL`
- 套餐：`体验版`
- PackageId：`baas_trial`
- 超额使用：关闭（`EnableOverrun=false`）
- 自动续费：关闭
- 当时显示到期时间：`2027-02-01 23:59:59`

“体验版且超额关闭”是 2026-08-02 的查询快照，不代表平台规则永远不变；涉及计费、套餐或到期时间时必须重新查询。无论如何，未经用户明确许可都不得开通或升级付费资源。

## 8. 静态托管

- 国内访问网址：`https://a1-d4gbxgxqmc01c0e55-1462114738.tcloudbaseapp.com/`
- 首页文档：`index.html`
- 错误文档：`index.html`
- 404/前端路由回退：`index.html`
- 继续使用现有 `manageHosting` 静态托管方式，不切换到会产生新域名的其他 Web App 部署方式。
- 用户截图里曾出现把完整网址重复拼到域名后的错误地址；正确入口始终是上面的根网址。

## 9. 标准更新与部署流程

### 只改前端

1. 在实际工程目录 `study-checkin-cloudbase/` 中运行 `npm install`。
2. 运行 `npm run build`。
3. 确认构建成功且 `dist/index.html` 存在。
4. 通过 CloudBase MCP 确认当前绑定环境仍是 `a1-d4gbxgxqmc01c0e55`。
5. 只把 `dist` 上传到现有静态托管根目录 `/`。
6. 打开正确网址进行桌面和手机端验收；若 CDN 有缓存，强制刷新后再判断。

### 改云函数

1. 修改 `functions/study-checkin-api/` 内的现有代码，不新建同类函数。
2. 安装/锁定 `package.json` 依赖。
3. 部署到同名 `study-checkin-api`，Node.js 18 或更高，入口 `index.main`。
4. 重新确认函数权限仍是默认禁止、仅认证用户可调用该函数。
5. 做蔡/刘双身份端到端验证。

### 涉及数据库结构

1. 先读取现有集合与索引；已存在则复用。
2. 只在上述四个集合内做必要的向后兼容修改。
3. 不开放客户端数据库权限。
4. 涉及历史数据时先备份/读取并设计可逆修复，不按数组位置批量重写任务语义。

## 10. 必做验收

- 页面能从正确国内网址打开。
- 桌面端和手机端布局正常；手机端能切换蔡/刘。
- 蔡和刘不需要共享码/PIN 即可选择身份。
- 各自能编辑、添加、删除和排序当天任务，保存后另一方能看到。
- 排序前后的完成记录与任务学习时间仍跟随同一个任务语义。
- 双方分别打卡后，进度和对方视图同步正确。
- 普通自习：开始、暂停、继续、结束正确。
- 番茄钟：自定义时长、开始、暂停、继续、阶段显示和结束正确。
- 自己的今日累计和对方的今日累计均显示正确；暂停期间不增长。
- 页面隐藏后不继续发周期性后端请求；回到前台可刷新。
- 刷新网页后主标题换成另一条语录，且不调用数据库获取语录。
- 四个集合仍为 `ADMINONLY`，函数仍只允许已认证用户调用。
- 套餐仍为体验版且超额关闭；若状态变化或操作可能收费，立即停止。

## 11. 历史需求时间线（便于理解为什么现在如此）

1. 最初目标是把现有双人学习打卡项目部署到已有 CloudBase 环境，并保留原有共享码/PIN、四项打卡和双人逻辑。
2. 用户纠正环境 ID 为 `a1-d4gbxgxqmc01c0e55`。
3. 产品方向随后明确改变：取消共享码/PIN，直接选择两个身份；用户一有学习任务，用户二待定；允许自定义每日任务并互看。
4. 身份最终命名为蔡和刘，并删除多段解释性占位文案。
5. 左侧导航栏被认为没有当前用途，因此删除。
6. 任务装饰图案改为数字序号。
7. 讨论并实现主界面自习模式；兼顾手机端，同时修复手机身份切换按钮消失问题。
8. 页面弱化任务/进度，强化自习和对方状态以增加陪伴感。
9. 因体验版每月资源点有限，逐步形成“本地计时、低频轮询、后台停止、动作才写入”的策略。
10. 加入普通自习/番茄钟、自定义番茄时长，以及两种模式的暂停/继续。
11. 同步频率最终确定为可见页面每 3 分钟一次，取消刷新次数上限。
12. 陪伴文案统一为“随时开始，对方会看到你的学习状态”。
13. 收集 1000 条大学生取向励志语录，并改为每次打开/刷新静态随机展示。
14. 增加自己的今日自习时间显示。
15. 用户发现任务无法排序且重新改标题造成完成记录错位；随后增加排序功能、修复稳定 ID 与当日历史数据。
16. 当前任务是把这次长对话中的全部有效知识打包，便于创建新项目后继续工作。
17. 修复对方今日计划和今日自习时长不会自动更新的问题：轻量轮询增加汇总修订号，仅在实际汇总变化时触发一次完整刷新，避免把每次轮询都升级为高消耗读取。
18. 按用户要求把新学习日的界限从自然日零点改为北京时间早上 5 点，并补充页面跨 05:00 自动换日、后台停止请求和回到前台立即刷新逻辑。
19. 发布 `v1.5.0`：任务编辑新增“每天重复”个人模板；新增首次打开显示的版本化更新日志，并补录 `v1.0.0` 起的主要历史版本。
20. 发布 `v1.6.0`：新增桌面/手机侧边栏，把用户切换移入侧边栏；新增按月历史学习时长日历并将“本周坚持”完成勾整合到日期；新增静态版本检测，检测到新版本时提示刷新网址。

## 12. 已知的历史/非权威文件

实际前端工程里的 `README.md` 和 `CODEX_DEPLOY_PROMPT.txt` 是项目早期文档，仍包含共享码、PIN 和初始任务方案。除非用户明确要求重写历史，它们可以保留，但不能作为当前需求来源；本文和根目录 `AGENTS.md` 优先级更高。

## 13. 秘密与未打包内容

- `.env.production` 仅记录变量名：`VITE_CLOUDBASE_ENV_ID`、`VITE_CLOUDBASE_REGION`、`VITE_CLOUDBASE_ACCESS_KEY`。
- 环境 ID 可公开用于配置；访问密钥不可复制进迁移包。
- `node_modules`、`dist`、本地日志和任何临时凭证都不属于知识迁移包。
- 源代码仍在本工作区内；最完整的转移方式是把整个工作区作为新 Codex 项目的文件夹，而不是只上传聊天文本。

## 14. 新对话的推荐首句

```text
请先完整阅读根目录 AGENTS.md 和 PROJECT_CONTEXT.md，再检查当前代码和 CloudBase 实际状态后继续。不要创建新 CloudBase 环境，不要开通或升级任何付费资源；保持任务稳定 ID、客户端数据库禁写和低资源点同步策略。
```
